node_modules/
dist/
.DS_Store
*.log
.vite/
*.tsbuildinfo
.wrangler/
.dev.vars

# direnv local overrides
.envrc.local
.direnv/

# project-isolated gh CLI config — contains OAuth tokens, must NEVER be committed
.gh/
